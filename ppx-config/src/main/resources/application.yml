spring:
  application:
    name: ppx-config
  cloud:
    config:
      server:
        git:
          username: root
          password: root
          uri: https://github.com/ATai2/ppxbi/tree/master/ppx-config/src/main/resources
          search-paths: /properties
server:
  port: 7001

