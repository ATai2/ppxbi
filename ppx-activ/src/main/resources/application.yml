server:
  port: 8920

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


spring:
  activiti:
    check-process-definitions: true #自动检查、部署流程定义文件
    database-schema-update: true #自动更新数据库结构
    process-definition-location-prefix: classpath:/processes/ #流程定义文件存放目录
  application:
    name: ppx-activiti

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/atdb?characterEncoding=utf8&useSSL=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver


#  jpa:
#    hibernate:
#      ddl-auto: update


#    spring.datasource.driver-class-name=com.mysql.jdbc.Driver
#    spring.datasource.url=jdbc:mysql://127.0.0.1:3306/activiti-demo?characterEncoding=utf8&useSSL=true
#    spring.datasource.username=root
#    spring.datasource.password=ROOT
#    spring.jpa.properties.hibernate.hbm2ddl.auto=update
#    spring.jpa.show-sql=true
#    server.port=8081
#    server.context-path=/
#    server.session.timeout=10
#    server.tomcat.uri-encoding=UTF-8
